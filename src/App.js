import React from 'react';

class App extends React.Component {
  render() {
    return <h1>Hello, world!</h1>;
  }
}

export default App;

// This code defines a new React component called 'App' that renders a <h1> element
// It then exports the App component so that it can be imported and used in othet parts of the application